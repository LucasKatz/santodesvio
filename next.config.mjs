/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ['10.160.4.110', 'localhost:3000'],
};

export default nextConfig;
